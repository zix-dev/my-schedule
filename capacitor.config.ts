import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.zix.myschedule',
  appName: 'my-schedule',
  webDir: 'dist/my-schedule',
  bundledWebRuntime: false
};

export default config;
